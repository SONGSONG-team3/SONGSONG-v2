<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.songsong.music.playlist.dao.PlaylistDao">
    <!-- 카테고리별 플레이리스트 목록 조회 -->
    <select id="selectPlaylistsByCategory">
        SELECT p.user_no, p.music_id
        FROM playlist p
                 JOIN music m ON p.music_id = m.music_id
        WHERE m.category_id = #{searchCategory}
        LIMIT #{offset}, #{limit}
    </select>
    <!-- 카테고리별 플레이리스트 수 조회 -->
    <select id="getPlaylistCountByCategory">
        SELECT COUNT(*)
        FROM playlist p
                 JOIN music m ON p.music_id = m.music_id
        WHERE m.category_id = #{categoryId}
    </select>
    <select id="getPlaylistCountByUserNo">
        SELECT COUNT(*)
        FROM playlist p
        WHERE p.user_no = #{userNo}
    </select>

</mapper>
